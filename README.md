# Legal CRM — прототип для юриста

Рабочий MVP: мини-CRM для ведения клиентов юриста.  
Собран за несколько часов как тестовое задание для «Доступного права».

## Что реализовано

- Таблица клиентов с кликом на строку для раскрытия деталей
- Добавление клиента: имя, телефон, статус, заметка
- Изменение статуса прямо в таблице (Новый → В работе → Закрыт)
- Визуальный прогресс-индикатор этапов дела
- Счётчики по статусам + фильтрация по кнопке
- Поиск по имени и телефону
- Заметки по делу с авто-сохранением
- Toast-уведомления при действиях
- Данные сохраняются в localStorage
- Бонус: serverless endpoint `/api/notify` — Telegram-уведомление при добавлении клиента

## Стек

React + Vite + localStorage. Без бэкенда, без БД — максимально быстрый MVP.  
Telegram-уведомление через Vercel serverless function.

## Запуск локально

```bash
npm install
npm run dev
```

## Деплой на Vercel (5 минут)

1. Загрузить проект на GitHub (инструкция ниже)
2. Зайти на vercel.com → New Project → выбрать репозиторий
3. Build command: `npm run build`, Output directory: `dist`
4. Deploy

## Telegram-уведомления (бонус)

В настройках проекта на Vercel добавить переменные окружения:

- `TELEGRAM_BOT_TOKEN` — токен бота от @BotFather
- `TELEGRAM_CHAT_ID` — ID чата куда слать уведомления

Если переменные не заданы — приложение работает без уведомлений.

## Как создать Telegram-бота

1. Написать @BotFather в Telegram → /newbot → получить токен
2. Написать боту любое сообщение
3. Открыть `https://api.telegram.org/bot<TOKEN>/getUpdates` — найти `chat.id`

## Лог по заданию

Стек выбран за скорость: React + Vite дают рабочий прототип за несколько часов,  
localStorage убирает необходимость в бэкенде на этапе MVP,  
Vercel позволяет опубликовать за пару кликов.

Что делал сам: структуру, UX-сценарий, фильтрацию, поиск, прогресс-индикатор, заметки, адаптивность, тестирование.  
AI помог: сгенерировал черновики компонентов, стилей, serverless endpoint, ускорил итерации.
